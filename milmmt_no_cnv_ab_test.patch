diff -ruN /mnt/data/videonotes_work/release_milmmt_official/backend/src/summary.rs /mnt/data/videonotes_work/test_no_cnv/backend/src/summary.rs
--- /mnt/data/videonotes_work/release_milmmt_official/backend/src/summary.rs	2026-08-22 06:44:32.091331120 +0000
+++ /mnt/data/videonotes_work/test_no_cnv/backend/src/summary.rs	2026-08-22 07:48:34.409912191 +0000
@@ -751,8 +751,8 @@
         .args(["--model"])
         .arg(model)
         // MiLMMT's model card uses a raw translation prompt with no chat protocol.
-        // Keep --prompt present only because llama.cpp requires it for --single-turn;
-        // the actual official prompt is supplied via --file.
+        // This test explicitly disables llama.cpp conversation mode with -no-cnv.
+        // Keep the existing prompt/file wiring unchanged so this is a one-variable A/B test.
         .args(["--prompt", " "])
         .args(["--file"])
         .arg(prompt_path)
@@ -785,7 +785,7 @@
             "42",
             "--reasoning",
             "off",
-            "--single-turn",
+            "-no-cnv",
             "--no-display-prompt",
             "--no-show-timings",
             "--no-warmup",
