# llama.cpp worker

Run `pnpm tools:fetch-llama` from the project root before building an installer. The pinned CPU worker and its DLLs are placed in this directory; model files are downloaded separately by the installed app.
