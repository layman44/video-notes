[permissions]
allow = ["Bash=cd src-tauri && cargo test 2>&1 | tail -10", "Bash=cd src-tauri && cargo test 2>&1 | grep -E \"^test |test result\" | head -30", "Bash=cd src-tauri && cargo clippy --all-targets 2>&1 | grep -E \"^warning|^error\" | head -10", "Bash=cd src-tauri && cargo fmt --check 2>&1 | head -30", "Bash=pnpm tauri build 2>&1 | tail -60", "Bash=curl -s \"https://huggingface.co/api/models/ggerganov/whisper.cpp\" > /tmp/hf-whisper.json && python -c \"\nimport json\ndata = json.load(open('/tmp/hf-whisper.json'))\nfiles = [f['rfilename'] for f in data.get('siblings', [])]\nturbo = sorted(f for f in files if 'turbo' in f.lower() and f.endswith('.bin'))\nlarge3 = sorted(f for f in files if 'large-v3' in f.lower() and f.endswith('.bin'))\nmedium = sorted(f for f in files if 'medium' in f.lower() and f.endswith('.bin'))\nprint('=== large-v3-turbo ===')\nfor f in turbo: print(f)\nprint('=== large-v3 ===')\nfor f in large3[:12]: print(f)\nprint('=== medium ===')\nfor f in medium: print(f)\n\"", "Bash=curl -s --max-time 30 \"https://hf-mirror.com/api/models/ggerganov/whisper.cpp\" > /tmp/hf-whisper.json && python -c \"\nimport json\ndata = json.load(open('/tmp/hf-whisper.json'))\nfiles = [f['rfilename'] for f in data.get('siblings', [])]\nturbo = sorted(f for f in files if 'turbo' in f.lower() and f.endswith('.bin'))\nlarge3 = sorted(f for f in files if 'large-v3' in f.lower() and f.endswith('.bin'))\nmedium = sorted(f for f in files if 'medium' in f.lower() and f.endswith('.bin'))\nprint('total files:', len(files))\nprint('=== large-v3-turbo ===')\nfor f in turbo: print(f)\nprint('=== large-v3 ===')\nfor f in large3[:14]: print(f)\nprint('=== medium ===')\nfor f in medium: print(f)\n\"", "Bash=curl -s --max-time 20 -o hf-whisper.json -w \"HTTP %{http_code}, %{size_download} bytes\\n\" \"https://hf-mirror.com/api/models/ggerganov/whisper.cpp\" ; ls -la hf-whisper.json 2>/dev/null", "Bash=python -c \"\nimport json\ndata = json.load(open('hf-whisper.json'))\nprint('keys:', list(data.keys()))\nfiles = [f['rfilename'] for f in data.get('siblings', [])]\nprint('num files:', len(files))\nprint('sample:', files[:5])\n\""]
